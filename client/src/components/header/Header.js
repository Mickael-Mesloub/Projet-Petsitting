import { Link } from "react-router-dom";
import "./styles.scss";

const Header = () => {
  return (
    <header>
      <h1 className="main-title">RubieLand</h1>
    </header>
  );
};

export default Header;
